{
  "name": "@fridaycode/tools",
  "version": "0.1.0",
  "description": "Built-in tools for file operations, search, git, browser, and shell execution",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "README.md", "LICENSE"],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@fridaycode/core": "workspace:*",
    "@fridaycode/providers": "workspace:*",
    "@fridaycode/shared": "workspace:*",
    "fast-glob": "^3.3.3",
    "puppeteer-core": "^24.40.0",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^2.0.0"
  },
  "author": "katipally",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/katipally/fridaycode.git",
    "directory": "packages/tools"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": ["fridaycode", "tools", "file-operations", "git", "shell", "browser"]
}
